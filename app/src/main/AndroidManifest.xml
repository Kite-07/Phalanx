<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- SMS permissions -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.RECEIVE_WAP_PUSH" />

    <!-- Network permissions (required for MMS) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Contact permissions -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Phone state for dual SIM support -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Notification permissions for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permission to bypass Do Not Disturb mode -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Storage permissions for MMS attachments -->
    <!-- For Android 13+ (API 33+) use granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- For Android 12 and below use legacy storage permission -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Phalanx">
        <activity
            android:name="com.kite.phalanx.SmsListActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent filter to handle SMS_DELIVER action (required for default SMS app) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.kite.phalanx.SmsDetailActivity"
            android:exported="false" />
        <activity
            android:name="com.kite.phalanx.ContactPickerActivity"
            android:exported="false" />
        <activity
            android:name="com.kite.phalanx.SpamListActivity"
            android:exported="false" />
        <activity
            android:name="com.kite.phalanx.SpamDetailActivity"
            android:exported="false" />
        <activity
            android:name="com.kite.phalanx.SettingsActivity"
            android:exported="false" />
        <activity
            android:name="com.kite.phalanx.ContactDetailActivity"
            android:exported="false" />
        <activity
            android:name="com.kite.phalanx.ArchivedMessagesActivity"
            android:exported="false" />

        <!-- SMS Receiver for incoming messages -->
        <receiver
            android:name="com.kite.phalanx.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
            </intent-filter>
        </receiver>

        <!-- MMS Receiver (required for default SMS app) -->
        <receiver
            android:name="com.kite.phalanx.MmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <!-- MMS Sent Status Receiver -->
        <receiver
            android:name="com.kite.phalanx.MmsSentReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kite.phalanx.MMS_SENT" />
            </intent-filter>
        </receiver>

        <!-- SMS Sent and Delivery Status Receiver -->
        <receiver
            android:name="com.kite.phalanx.SmsSentReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kite.phalanx.SMS_SENT" />
                <action android:name="com.kite.phalanx.SMS_DELIVERED" />
            </intent-filter>
        </receiver>

        <!-- Service to respond to requests from other parts of the system (required for default SMS app) -->
        <service
            android:name="com.kite.phalanx.HeadlessSmsSendService"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>

        <!-- Notification Action Receiver for Mark as Read and Reply -->
        <receiver
            android:name="com.kite.phalanx.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kite.phalanx.MARK_AS_READ" />
                <action android:name="com.kite.phalanx.REPLY" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing MMS attachments -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>
